# simulation config
  target_fps = 999
  full_screen = false
  window_width = 1280
  window_height = 720
  is_resizable = true
  should_be_profiled = true

# world config
  game_x = 50
  game_y = 50
  game_z = 50
  wall_elasticity = 0.8
  apply_gravity = false
  gravity = 9.81
  use_experimental_quadtree = false
  show_quadtree = false
  
# unit config
  unit_number = 250
  unit_initial_spacing = 1

  unit_radius = 1
  unit_radius_multiplier = 0.05
  set_random_radius = true
  radius_min = 1
  radius_max = 5
  
  unit_mass_multiplier = 1
  set_random_mass_multiplier = true
  mass_multiplier_min = 1
  mass_multiplier_max = 100

  units_emit_gravity = true
  unit_gravitational_multiplier = 1
  
  unit_elasticity = 0.8
  set_random_elasticity = true
  elasticity_min = 0.5
  elasticity_max = 0.7
  

  show_vectors = true
  scale_factor = 1 # 1 pixel = 1 cm
  show_trail = true
  show_overlay = true
  set_random_color = true
  show_speed_color = false
  
# cluster config
  unit_transition_duration = 5
  cluster_threshold = 1 # minimum distance in order to create a cluster
  cluster_resistance_factor = 50
